import { SignInButton } from '~/components/domain/auth/SignInButton';

export default function TopOptionsMenu() {
  return (
    <div className="flex bg-black w-full p-4">
      <SignInButton />
    </div>
  );
}
